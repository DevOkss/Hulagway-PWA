# The API base URL is a hardcoded constant in src/services/api.ts (API_BASE_URL).
# No env variable is needed — the PWA will always target the deployed backend:
#   https://hulagway.devokss.online/api
#
# This file exists only as a placeholder; .env / VITE_API_URL are intentionally
# NOT used so no Vercel environment variable has to be configured.
